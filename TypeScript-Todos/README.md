# Exercice TypeScript

- remplacer tous les querySelector et querySelectorAll par leur version TypeScript Generic (`querySelector<HTMLFormElement>('.class')`)

- créer et utiliser une interface Todo pour la fonction `createTodoRow`

- typer tous les paramètres d'entrées de nos fonctions lorsque c'est nécessaire (parfois automatique avec l'inférence de type)

- typer les variables `target`
